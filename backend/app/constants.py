# User constants
# This should only be used for testing, not in production code
# In production, always use the authenticated user's ID from the request
TEST_USER_ID = "isaac_mineo"  # Primary user ID for testing/demo purposes only
USER_ID = "isaac_mineo"  # Fallback user ID for compatibility with older code 